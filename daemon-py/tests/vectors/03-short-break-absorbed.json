{
  "name": "short_break_is_absorbed",
  "description": "Inactivity below min_inactivity keeps the active span continuous with no event.",
  "steps": [
    { "op": "tick", "now": 0, "idle_ms": 0 },
    { "op": "tick", "now": 40000, "idle_ms": 0 },
    { "op": "tick", "now": 300000, "idle_ms": 300000 }
  ],
  "expected": [
    { "kind": "active", "ts": 0 }
  ],
  "expected_final": { "reported_state": "Active" }
}
