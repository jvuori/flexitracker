{
  "name": "min_inactivity_exact_boundary_closes",
  "description": "idle_ms exactly equal to min_inactivity closes the span (>= boundary).",
  "steps": [
    { "op": "tick", "now": 0, "idle_ms": 0 },
    { "op": "tick", "now": 40000, "idle_ms": 0 },
    { "op": "tick", "now": 640000, "idle_ms": 600000 }
  ],
  "expected": [
    { "kind": "active", "ts": 0 },
    { "kind": "idle", "ts": 40000 }
  ],
  "expected_final": { "reported_state": "Idle" }
}
